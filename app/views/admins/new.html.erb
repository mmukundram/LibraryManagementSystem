<% if logged_in? and @user %>
    <div class="bs-docs-section clearfix">
      <div class="row">
        <div class="col-lg-12">
          <div class="page-header">
            <h1>Register New Admin</h1>
          </div>
          <%= render 'form', user: @user %>
        </div>
      </div>
    </div>

    <%= link_to 'Cancel', admins_path, class: "btn btn-primary" %>

<% end %>